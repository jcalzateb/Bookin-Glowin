import React from "react";

const Header = () => {
  return (
    <header>
      <h1>glowin</h1>
      <nav>
        <button>Crear cuenta</button>
        <button>Iniciar sesión</button>
      </nav>
    </header>
  );
};

export default Header;
